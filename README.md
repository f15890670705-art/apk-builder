# apk-builder

Assorted Android build helpers. CI assembles the toolchain on Linux runners and drops a heartbeat into `build_ok.txt` whenever a run goes green.

Builds are triggered manually from the Actions tab.

Mostly infrastructure glue - nothing interesting to see here.
